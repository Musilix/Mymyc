import { BankingScenarioSource } from './banking-scenario-source';

describe('BankingScenarioSource', () => {
  it('should create an instance', () => {
    expect(new BankingScenarioSource()).toBeTruthy();
  });
});
